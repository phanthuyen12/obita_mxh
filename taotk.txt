php artisan tinker --execute 'foreach ([["name" => "Admin", "email" => "admin@kingpost.com", "password" => "kingpost2026"], ["name" => "Phan Gia Thuyen Dev", "email" => "phangiathuyendev@gmail.com", "password" => "thuyen2004@T"]] as $credentials) { $user = App\Models\User::where("email", $credentials["email"])->first(); if (! $user) { $user = App\Actions\User\CreateUser::execute(["name" => $credentials["name"], "email" => $credentials["email"], "password" => $credentials["password"], "email_verified_at" => now()]); } else { $user->update(["name" => $credentials["name"], "password" => $credentials["password"], "email_verified_at" => $user->email_verified_at ?? now()]); } if (! $user->current_workspace_id) { App\Actions\Workspace\CreateWorkspace::execute($user, ["name" => $credentials["name"]." Workspace"]); } }'